<UserControl x:Class="UmbracoDiff.Views.Settings.UmbracoConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Expander Header="{Binding Header}">
        <StackPanel Orientation="Vertical">
            <TextBox x:Name="Name" Margin="10" controls:TextboxHelper.Watermark="Name ..." />
            <TextBox x:Name="ConnectionString" Margin="10" controls:TextboxHelper.Watermark="Connection String ..." />
            <Button x:Name="Save" Margin="15" Content="Save" />
        </StackPanel>
    </Expander>

</UserControl>
