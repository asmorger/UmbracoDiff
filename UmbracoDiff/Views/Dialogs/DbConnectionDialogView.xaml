<UserControl x:Class="UmbracoDiff.Views.Dialogs.DbConnectionDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
          <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions> 

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock x:Name="Name" Controls:TextboxHelper.Watermark="Name ..." />
            <TextBlock x:Name="ConnectionString" Controls:TextboxHelper.Watermark="Connection String ..." />
        </StackPanel>
        <Button Grid.Row="1" Margin="20" Content="Save" />
    </Grid>

</UserControl>
